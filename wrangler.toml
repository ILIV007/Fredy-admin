# ============================================================
# Fredy — Cloudflare Worker Configuration
# Version: 1.5.2
# ============================================================

name = "fredy-admin"
main = "src/index.ts"
compatibility_date = "2024-12-01"
compatibility_flags = ["nodejs_compat"]

# ============================================================
# KV Namespaces
# ============================================================
[[kv_namespaces]]
binding = "Fredy_SETTINGS"
id = "64f76bc1a74346cc969a007f3a322050"

# ============================================================
# Cron Triggers
# ============================================================
# Free tier: max 5 cron triggers per account.
# Using 1 trigger (every minute) to stay within limits.
# Source cache refresh happens within the same tick.

[triggers]
crons = ["* * * * *"]

# ============================================================
# Environment Variables (Non-Secret)
# ============================================================
[vars]
TARGET_CHANNEL = "@ILIVIR3"
FOOTER_TEXT = "🌀 @ILIVIR3"
DEBUG_MODE = "false"
DEFAULT_AI_PROVIDER = "gemini"
DEFAULT_LANGUAGE = "auto"
SCHEDULER_TIMEZONE = "Asia/Tehran"
SCHEDULE_SLOTS = "09:00,13:00,18:00,22:00"
SCHEDULE_JITTER_MINUTES = "30"

# ============================================================
# Secrets (set via `wrangler secret put` or dashboard):
#   BOT_TOKEN              — from @BotFather
#   ADMIN_ID               — your Telegram user ID
#   GEMINI_API_KEY         — from https://aistudio.google.com/apikey
#   OPENROUTER_API_KEY     — from https://openrouter.ai/keys
#   NEWSAPI_KEY            — from https://newsapi.org
#   NASA_API_KEY           — from https://api.nasa.gov
#   WEBHOOK_SECRET         — random string for webhook verification
#   DEBUG_TOKEN            — random string for /debug protection
#   GITHUB_TOKEN           — optional, for higher rate limit
# ============================================================

[observability]
enabled = true
